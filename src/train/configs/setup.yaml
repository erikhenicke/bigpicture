defaults:
  - _self_
  - data: fmow_multiscale
  - model: deit_both
  - optim: adamw_plateau
  - trainer: default
  - wandb: fmow
  - optional experiment: null

num_task_labels: 62
num_domain_labels: 6
domain_index: 0
seed: 111
num_reruns: 3 

run_name: late-fusion

wandb:
  run_name: ${run_name}-${now:%y-%m-%d-%H-%M-%S}

hydra:
  run:
    dir: log/runs/${now:%Y-%m-%d}/${run_name}-${now:%H-%M-%S}
