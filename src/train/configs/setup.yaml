defaults:
  - _self_
  - data: fmow_multiscale
  - spatial: no_spatial
  - optim: adamw_plateau
  - trainer: default
  - wandb: fmow
  - model: deit_both

num_task_labels: 62
domain_index: 0
seed: 111
num_reruns: 3 

run_name: late-fusion

hydra:
  run:
    dir: log/runs/${now:%Y-%m-%d}/${run_name}-${now:%H-%M-%S}
