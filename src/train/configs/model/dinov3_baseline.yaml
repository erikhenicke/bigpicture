# @package _global_
model:
  hr_encoder:
    _target_: models.components.branches.DINOv3Branch
    in_channels: 3
    pretrained: true
    mode_size: base

  model:
    _target_: models.components.fusion_model.SingleBranchModel

run_name: train-dinov3-baseline
data:
  batch_size: 64 
trainer:
  max_epochs: 10
