# @package _global_
model:
  checkpoint_path: artifacts/pretrained/dinov3_vitl16_pretrain_sat493m-eadcf0ff.pth

  hr_encoder:
    _target_: models.components.branches.DINOv3Branch
    in_channels: 3
    checkpoint_path: ${model.checkpoint_path}

  model:
    _target_: models.components.fusion_model.SingleBranchModel

run_name: train-dinov3-baseline
data:
  batch_size: 16
