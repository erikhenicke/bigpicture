# @package _global_
model:
  hr_encoder:
    _target_: models.components.branches.SatCLIPImageBranch
    in_channels: 3
    pretrained: true
    num_unfreeze_last: 0
    unfreeze_first_block: false

  model:
    _target_: models.components.fusion_model.SingleBranchModel

run_name: train-satclip-image-enc-baseline
data:
  batch_size: 128
