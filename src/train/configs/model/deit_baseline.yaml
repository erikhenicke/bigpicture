# @package _global_
model:
  hr_encoder:
    _target_: models.components.branches.DeitBranch
    in_channels: 3
    pretrained: true

  model:
    _target_: models.components.fusion_models.SingleBranchModel

run_name: train-deit-baseline
data:
  batch_size: 128
