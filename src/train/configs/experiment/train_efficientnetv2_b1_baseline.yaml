# @package _global_
defaults:
  - override /model: efficientnetv2_b1_baseline
  - override /optim: adam_cosine

run_name: train-efficientnetv2-b1-baseline

trainer:
  max_epochs: 50

data:
  batch_size: 64
  frac: 1.0
  augment_train: true
