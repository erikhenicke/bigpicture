# @package _global_
defaults:
  - override /model: densenet_both
  - override /optim: adam_warmup_cosine

run_name: train-densenet-concat

trainer:
  max_epochs: 50

data:
  batch_size: 64
  frac: 1.0
  augment_train: true

model:
  hr_image_net: true
  lr_image_net: true
