# @package _global_
defaults:
  - override /optim: adam_no_scheduler

run_name: train-baseline-like

trainer:
  max_epochs: 100

data:
  batch_size: 64
  frac: 1.0
  augment_train: false

model:
  hr_image_net: true
  lr_image_net: true
